name: silversheet
windows:
  - name: rails-console
    root: ~/code/silver-sheet
    panes:
      - rails console
  - name: server-sidekiq-npm
    root: ~/code/silver-sheet
    layout: tiled
    panes:
      - be sidekiq
      - npm start
      - rails server -p 5000
